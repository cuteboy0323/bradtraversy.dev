---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const ACTIVE_STATUSES = ['alpha', 'beta', 'live', 'local'];
const allProjects = await getCollection('projects');
const currentlyBuilding = allProjects
  .filter((p) => p.data.featured || ACTIVE_STATUSES.includes(p.data.status))
  .sort((a, b) => {
    if (a.data.featured !== b.data.featured) return a.data.featured ? -1 : 1;
    return b.data.publishDate.valueOf() - a.data.publishDate.valueOf();
  })
  .slice(0, 6);

// Manual focus notes per project (1-line "what's the active push here").
// Update these when the focus shifts; if a project isn't listed, the card
// still renders with its tagline as fallback.
const focusNotes: Record<string, string> = {
  memcrate:
    'v0.3.3 shipped to crates.io. memcrate.dev landing + docs + pagefind search live. next is more skill packs (cursor, aider, mcp) on top of the format.',
  vidpipe:
    'channel-connection pivot live. transcript-provider fallback in. publishing destinations (wordpress, medium, ghost) are next when i swing back.',
  'bradtraversy-dev':
    'launched — daily content polish, project + tool pages filling out, devlog cadence holding.',
  'mission-control':
    'running daily on the homelab. write-back on calendar + per-profile heartbeats next. paperclip will eventually take over the task lane.',
  devsheets:
    'on autopilot — sheets land via the create/audit/validate/sync skill chain when i pick a topic from the backlog.',
  webutils:
    'maintenance mode — adding a tool when i need one and can\'t find a clean version of it elsewhere.',
};

const lastUpdated = '2026-05-17';
---

<BaseLayout
  title="now — brad traversy"
  description="what i'm actively working on right now."
  pageTitle="now.md"
  currentSection="now"
>
  <section class="section">
    <h1 class="h1"><span class="hash"># </span>now</h1>
    <p class="lede">
      what i'm focused on right this minute. updated when the focus shifts —
      see the date at the bottom for the last revision.
    </p>
  </section>

  <section class="section">
    <h2 class="h2"><span class="hash">## </span>right now</h2>
    <p>
      deepest into <a href="https://stackmaven.io" class="link">stackmaven</a> —
      a curated catalog of dev/AI tools, models, and frameworks with a news
      layer and (later) a stack recommender. v0.2 alpha is live; phase 2 is
      catalog depth (15 → 250 entries) and sustained news cadence.
    </p>
    <p>
      on the homelab side, just cut over from a single autonomous agent
      (travis on openclaw) to a four-profile hermes setup — sysadmin,
      developer, creator, secretary — one runtime, one shared vault, four
      lanes. <a href="/devlog/2026-05-17-from-travis-to-hermes" class="link">devlog</a>.
    </p>
    <p>
      vidpipe is still real but slower-cadence now that the channel
      pivot is in. bradtraversy.dev keeps getting daily content polish —
      project + tool pages, devlog cadence, the homelab article. less
      youtube production than usual. more shipping.
    </p>
  </section>

  <section class="section">
    <h2 class="h2"><span class="hash">## </span>currently building</h2>
    <div class="comment">
      // featured + active projects, with the current push on each
    </div>
    <div class="now-projects">
      {currentlyBuilding.map((project) => (
        <a class="now-project" href={`/projects/${project.id}`}>
          <div class="now-project-head">
            <span class="now-project-name">{project.data.name}</span>
            <span class:list={['pill', project.data.status]}>{project.data.status}</span>
          </div>
          <p class="now-project-note">
            {focusNotes[project.id] ?? project.data.tagline}
          </p>
        </a>
      ))}
    </div>
    <a href="/projects" class="link more-link">→ all projects</a>
  </section>

  <section class="section">
    <h2 class="h2"><span class="hash">## </span>what i'm thinking about</h2>
    <ul class="prose-list">
      <li>
        whether specialization beats generalization for autonomous agents —
        one agent doing everything vs. four lanes that don't overlap. the
        hermes swap is the bet i'm making on this
      </li>
      <li>
        editorial-voice publication brands as standalone businesses — what
        does "the wirecutter for dev tools" look like when it's also an
        active recommender, not just a directory
      </li>
      <li>
        whether "indie builder + known person" beats "new company brand + same
        products" for distribution. (rebranding the public site from a brand
        name to my personal name was the bet i made on that one — stackmaven
        is the opposite bet, brand-first)
      </li>
      <li>
        what a vault-native dashboard looks like when the markdown <em>is</em>{' '}
        the database — and what an orchestration layer on top of it (paperclip)
        does that the dashboard doesn't
      </li>
    </ul>
  </section>

  <section class="section">
    <h2 class="h2"><span class="hash">## </span>what i'm not doing</h2>
    <ul class="prose-list">
      <li>no live coding sessions or live streams right now</li>
      <li>not chasing every shiny model release — only swap when there's a real reason</li>
      <li>not building a course platform — that's not what this site is for</li>
      <li>no new sponsor commitments beyond existing ones</li>
    </ul>
  </section>

  <section class="section">
    <p class="comment-prose">
      // last updated <strong>{lastUpdated}</strong>. inspired by{' '}
      <a href="https://nownownow.com/about" class="link">/now pages</a>.
    </p>
  </section>

  <div class="comment" style="margin-top: 24px;">
    // EOF now.md<span class="cursor"></span>
  </div>
</BaseLayout>

<style>
  .now-projects {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-top: 12px;
  }
  .now-project {
    display: block;
    padding: 14px 16px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--bg-2);
    text-decoration: none;
    color: var(--fg-1);
    transition: border-color 0.12s ease, background 0.12s ease;
  }
  .now-project:hover {
    border-color: var(--border-strong);
    background: var(--bg-3, var(--bg-2));
  }
  .now-project-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
  }
  .now-project-name {
    font-weight: 600;
    font-size: 14px;
  }
  .now-project-note {
    margin: 6px 0 0;
    color: var(--fg-muted);
    font-size: 13px;
    line-height: 1.5;
  }
  .prose-list {
    margin: 12px 0 16px;
    padding-left: 20px;
    list-style: disc;
    color: var(--fg-1);
  }
  .prose-list li {
    margin-bottom: 8px;
    line-height: 1.55;
  }
  .comment-prose {
    color: var(--fg-comment);
    font-size: 12.5px;
  }
</style>
